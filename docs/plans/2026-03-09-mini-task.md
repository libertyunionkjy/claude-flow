# Mini Task 功能实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 Claude Flow 添加 Mini Task 功能 — 一种轻量级交互式任务，跳过 plan/review 阶段，通过前端 Web 终端（xterm.js + PTY + WebSocket）在独立 worktree 中直接与 Claude Code 交互。

**Architecture:** Mini Task 复用现有的 Task 数据模型（通过 `task_type` 字段区分），在后端新增 PTY Session 管理器通过 WebSocket 桥接浏览器终端，前端侧边栏新增独立的 Mini Task 区域。CLI 端仅负责创建任务和分配 worktree，所有交互在前端完成。

**Tech Stack:** Python 3.10+, Flask, flask-sock (WebSocket), pty/os (PTY 管理), xterm.js 5.x (本地静态文件), 现有 Task/TaskManager/WorktreeManager 基础设施。

---

## 设计决策汇总

| # | 决定项 | 结论 |
|---|--------|------|
| 1 | Worktree 隔离 | 是，每个 mini task 独立 worktree |
| 2 | 执行模式 | 仅交互式（在 worktree 中启动 `claude` session） |
| 3 | 多任务并行 | 前端侧边栏标签管理 |
| 4 | 生命周期 | `pending → running → merging → done`（跳过 plan/review） |
| 5 | 终端实现 | PTY + WebSocket + xterm.js |
| 6 | 侧边栏位置 | Task Status 之后、Usage/Guide 之前 |
| 7 | 创建方式 | CLI + 前端双入口 |
| 8 | 合并流程 | 手动确认，前端展示 diff 预览 |
| 9 | 数据存储 | 统一 tasks.json，`task_type: "mini"` 区分 |
| 10 | CLI 行为 | 仅创建任务，交互在前端完成 |
| 11 | PTY 生命周期 | 持久化，可重新连接 |
| 12 | Worktree 清理 | 合并后自动清理 |
| 13 | Prompt 处理 | 自动发送为第一条消息 |
| 14 | xterm.js 引入 | 本地静态文件 |
| 15 | 标题 | 必填 |
| 16 | CLI 命令结构 | `cf mini add "标题" -p "prompt"` + `cf mini list` |
| 17 | 服务器重启恢复 | 标记为 interrupted，用户手动重启 |

---

## Task 1: 扩展数据模型 — 添加 `task_type` 和 `INTERRUPTED` 状态

**Files:**
- Modify: `claude_flow/models.py:10-82`
- Test: `tests/test_models.py`

**Step 1: 写失败测试**

在 `tests/test_models.py` 末尾新增测试：

```python
def test_task_type_default():
    """standard task defaults to task_type='standard'."""
    t = Task(title="test", prompt="test prompt")
    assert t.task_type == "standard"

def test_task_type_mini():
    """mini task can be created with task_type='mini'."""
    t = Task(title="mini", prompt="p", task_type="mini")
    assert t.task_type == "mini"

def test_task_type_roundtrip():
    """task_type survives to_dict/from_dict."""
    t = Task(title="rt", prompt="p", task_type="mini")
    d = t.to_dict()
    assert d["task_type"] == "mini"
    restored = Task.from_dict(d)
    assert restored.task_type == "mini"

def test_task_type_default_from_dict():
    """from_dict without task_type defaults to 'standard' (backward compat)."""
    d = {
        "id": "task-abc",
        "title": "Old",
        "prompt": "P",
        "status": "pending",
        "created_at": "2026-03-09T00:00:00",
    }
    t = Task.from_dict(d)
    assert t.task_type == "standard"

def test_interrupted_status():
    """INTERRUPTED status exists and has correct value."""
    assert TaskStatus.INTERRUPTED.value == "interrupted"

def test_interrupted_roundtrip():
    """INTERRUPTED status survives serialization."""
    t = Task(title="t", prompt="p", status=TaskStatus.INTERRUPTED)
    d = t.to_dict()
    assert d["status"] == "interrupted"
    restored = Task.from_dict(d)
    assert restored.status == TaskStatus.INTERRUPTED
```

**Step 2: 运行测试验证失败**

Run: `pytest tests/test_models.py -v -k "task_type or interrupted"`
Expected: FAIL — `Task` 没有 `task_type` 字段，`TaskStatus` 没有 `INTERRUPTED`

**Step 3: 实现数据模型扩展**

修改 `claude_flow/models.py`：

1. 在 `TaskStatus` 枚举中（`FAILED` 之后）添加:
```python
INTERRUPTED = "interrupted"
```

2. 在 `Task` dataclass 中添加字段（`plan_mode` 之后）:
```python
task_type: str = "standard"  # "standard" | "mini"
```

3. 在 `to_dict()` 方法的返回字典中添加:
```python
"task_type": self.task_type,
```

4. 在 `from_dict()` 方法的 `cls(...)` 调用中添加:
```python
task_type=d.get("task_type", "standard"),
```

**Step 4: 运行测试验证通过**

Run: `pytest tests/test_models.py -v`
Expected: ALL PASS

**Step 5: 运行全量测试确保无回归**

Run: `pytest -v`
Expected: ALL PASS — 现有测试中 `from_dict` 的字典没有 `task_type` 字段，但 `.get("task_type", "standard")` 默认值保证向后兼容。

**Step 6: Commit**

```bash
git add claude_flow/models.py tests/test_models.py
git commit -m "feat(models): add task_type field and INTERRUPTED status for mini task support"
```

---

## Task 2: TaskManager 扩展 — `add_mini()` 方法和类型筛选

**Files:**
- Modify: `claude_flow/task_manager.py:100-115`
- Test: `tests/test_task_manager.py`

**Step 1: 写失败测试**

在 `tests/test_task_manager.py` 中新增测试：

```python
def test_add_mini(tmp_path):
    """add_mini creates a task with task_type='mini' and status=PENDING."""
    cf_dir = tmp_path / ".claude-flow"
    cf_dir.mkdir()
    tm = TaskManager(tmp_path)
    t = tm.add_mini(title="Fix typo", prompt="Fix the typo in README")
    assert t.task_type == "mini"
    assert t.status == TaskStatus.PENDING
    assert t.title == "Fix typo"
    assert t.prompt == "Fix the typo in README"

def test_list_tasks_by_type(tmp_path):
    """list_tasks can filter by task_type."""
    cf_dir = tmp_path / ".claude-flow"
    cf_dir.mkdir()
    tm = TaskManager(tmp_path)
    tm.add("Standard", "s prompt")
    tm.add_mini("Mini", "m prompt")
    tm.add("Standard2", "s2 prompt")

    all_tasks = tm.list_tasks()
    assert len(all_tasks) == 3

    minis = tm.list_tasks(task_type="mini")
    assert len(minis) == 1
    assert minis[0].title == "Mini"

    standards = tm.list_tasks(task_type="standard")
    assert len(standards) == 2
```

**Step 2: 运行测试验证失败**

Run: `pytest tests/test_task_manager.py -v -k "add_mini or list_tasks_by_type"`
Expected: FAIL — `add_mini` 方法不存在，`list_tasks` 不接受 `task_type` 参数

**Step 3: 实现 TaskManager 扩展**

修改 `claude_flow/task_manager.py`：

1. 在 `add()` 方法之后（约 line 107），添加 `add_mini()` 方法:
```python
def add_mini(self, title: str, prompt: str) -> Task:
    """Add a mini task (interactive, skips plan/review)."""
    def _do():
        tasks = self._load()
        task = Task(title=title, prompt=prompt, task_type="mini")
        tasks.append(task)
        self._save(tasks)
        return task
    return self._with_lock(_do)
```

2. 修改 `list_tasks()` 签名（约 line 109），增加 `task_type` 可选参数:
```python
def list_tasks(
    self, status: Optional[TaskStatus] = None, task_type: Optional[str] = None
) -> List[Task]:
    def _do():
        tasks = self._load()
        if status:
            tasks = [t for t in tasks if t.status == status]
        if task_type:
            tasks = [t for t in tasks if t.task_type == task_type]
        return tasks
    return self._with_shared_lock(_do)
```

**Step 4: 运行测试验证通过**

Run: `pytest tests/test_task_manager.py -v`
Expected: ALL PASS

**Step 5: 运行全量测试确保无回归**

Run: `pytest -v`
Expected: ALL PASS — 现有调用 `list_tasks(status=...)` 不受 `task_type` 新参数影响（默认 None）。

**Step 6: Commit**

```bash
git add claude_flow/task_manager.py tests/test_task_manager.py
git commit -m "feat(task-manager): add add_mini() and task_type filtering for mini tasks"
```

---

## Task 3: PTY Session 管理器 — 后端核心

**Files:**
- Create: `claude_flow/pty_manager.py`
- Test: `tests/test_pty_manager.py`

**Step 1: 写失败测试**

创建 `tests/test_pty_manager.py`：

```python
"""PTY Manager unit tests.

PTY fork operations are mocked since they require a real terminal environment.
Tests verify the session lifecycle management logic.
"""
from __future__ import annotations

import os
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

from claude_flow.pty_manager import PtyManager, PtySession


class TestPtySession:
    def test_session_creation(self):
        """PtySession stores task_id, pid, fd."""
        s = PtySession(task_id="task-abc", pid=1234, fd=5, wt_path=Path("/tmp/wt"))
        assert s.task_id == "task-abc"
        assert s.pid == 1234
        assert s.fd == 5
        assert s.alive is True

    def test_session_mark_dead(self):
        """Session can be marked as dead."""
        s = PtySession(task_id="task-abc", pid=1234, fd=5, wt_path=Path("/tmp/wt"))
        s.alive = False
        assert s.alive is False


class TestPtyManager:
    def test_create_session(self, tmp_path):
        """create_session stores a PtySession and returns it."""
        mgr = PtyManager()
        wt_path = tmp_path / "worktree"
        wt_path.mkdir()

        with patch("claude_flow.pty_manager.pty.fork") as mock_fork, \
             patch("claude_flow.pty_manager.os.execvpe") as mock_exec:
            # Simulate parent process return from fork
            mock_fork.return_value = (1234, 5)  # (pid, fd)
            session = mgr.create_session("task-abc", wt_path, prompt="hello")

        assert session.task_id == "task-abc"
        assert session.pid == 1234
        assert session.fd == 5
        assert "task-abc" in mgr._sessions

    def test_get_session(self, tmp_path):
        """get_session returns existing session or None."""
        mgr = PtyManager()
        assert mgr.get_session("nonexistent") is None

        # Manually add a session
        s = PtySession(task_id="task-x", pid=99, fd=3, wt_path=tmp_path)
        mgr._sessions["task-x"] = s
        assert mgr.get_session("task-x") is s

    def test_remove_session(self, tmp_path):
        """remove_session kills process and removes from registry."""
        mgr = PtyManager()
        s = PtySession(task_id="task-y", pid=99, fd=3, wt_path=tmp_path)
        mgr._sessions["task-y"] = s

        with patch("claude_flow.pty_manager.os.kill") as mock_kill, \
             patch("claude_flow.pty_manager.os.close") as mock_close, \
             patch("claude_flow.pty_manager.os.waitpid") as mock_wait:
            mock_wait.return_value = (99, 0)
            mgr.remove_session("task-y")

        assert "task-y" not in mgr._sessions
        mock_kill.assert_called_once()
        mock_close.assert_called_once_with(3)

    def test_list_sessions(self, tmp_path):
        """list_sessions returns all task_ids."""
        mgr = PtyManager()
        mgr._sessions["a"] = PtySession(task_id="a", pid=1, fd=1, wt_path=tmp_path)
        mgr._sessions["b"] = PtySession(task_id="b", pid=2, fd=2, wt_path=tmp_path)
        assert set(mgr.list_sessions()) == {"a", "b"}

    def test_recover_marks_interrupted(self, tmp_path):
        """recover_sessions marks all existing sessions as dead."""
        mgr = PtyManager()
        s = PtySession(task_id="task-z", pid=99, fd=3, wt_path=tmp_path)
        mgr._sessions["task-z"] = s
        dead = mgr.recover_sessions()
        assert dead == ["task-z"]
        assert s.alive is False
```

**Step 2: 运行测试验证失败**

Run: `pytest tests/test_pty_manager.py -v`
Expected: FAIL — `claude_flow.pty_manager` 模块不存在

**Step 3: 实现 PtyManager**

创建 `claude_flow/pty_manager.py`：

```python
"""PTY session lifecycle manager for mini task interactive terminals.

Each mini task gets a dedicated PTY running `claude` CLI in its worktree.
Sessions persist across browser tab close/reopen (reattachable).
Server restart marks sessions as dead (INTERRUPTED).
"""
from __future__ import annotations

import logging
import os
import pty
import signal
from dataclasses import dataclass, field
from pathlib import Path
from typing import Dict, List, Optional

logger = logging.getLogger(__name__)


@dataclass
class PtySession:
    """Represents a single PTY session for a mini task."""
    task_id: str
    pid: int
    fd: int           # master fd for reading/writing
    wt_path: Path     # worktree directory
    alive: bool = True
    prompt: str = ""  # initial prompt sent to claude


class PtyManager:
    """Manages PTY sessions for mini tasks.

    Thread-safe via GIL for dict operations. Each session is a forked
    child process running `claude` in a worktree directory.
    """

    def __init__(self):
        self._sessions: Dict[str, PtySession] = {}

    def create_session(
        self,
        task_id: str,
        wt_path: Path,
        prompt: str = "",
        skip_permissions: bool = True,
    ) -> PtySession:
        """Fork a PTY child process running `claude` in the given worktree.

        Args:
            task_id: The mini task ID.
            wt_path: Path to the git worktree.
            prompt: Initial prompt to send after startup.
            skip_permissions: Whether to use --dangerously-skip-permissions.

        Returns:
            The created PtySession.
        """
        cmd = ["claude"]
        if skip_permissions:
            cmd.append("--dangerously-skip-permissions")

        pid, fd = pty.fork()

        if pid == 0:
            # Child process: cd to worktree and exec claude
            os.chdir(str(wt_path))
            os.execvpe(cmd[0], cmd, os.environ)
            # execvpe never returns on success
        else:
            # Parent process: store session
            session = PtySession(
                task_id=task_id,
                pid=pid,
                fd=fd,
                wt_path=wt_path,
                prompt=prompt,
            )
            self._sessions[task_id] = session
            logger.info(f"PTY session created for {task_id}, pid={pid}")
            return session

    def get_session(self, task_id: str) -> Optional[PtySession]:
        """Get session by task_id, or None."""
        return self._sessions.get(task_id)

    def remove_session(self, task_id: str) -> None:
        """Kill the PTY process and remove the session."""
        session = self._sessions.pop(task_id, None)
        if session is None:
            return

        try:
            os.kill(session.pid, signal.SIGTERM)
            os.waitpid(session.pid, 0)
        except (OSError, ChildProcessError):
            pass

        try:
            os.close(session.fd)
        except OSError:
            pass

        session.alive = False
        logger.info(f"PTY session removed for {task_id}")

    def list_sessions(self) -> List[str]:
        """Return list of active session task_ids."""
        return list(self._sessions.keys())

    def recover_sessions(self) -> List[str]:
        """Mark all sessions as dead after server restart.

        Returns list of task_ids that were interrupted.
        The caller should update their status to INTERRUPTED in TaskManager.
        """
        interrupted = []
        for task_id, session in self._sessions.items():
            session.alive = False
            interrupted.append(task_id)
        return interrupted

    def read(self, task_id: str, size: int = 4096) -> Optional[bytes]:
        """Read from PTY master fd (non-blocking safe)."""
        session = self._sessions.get(task_id)
        if not session or not session.alive:
            return None
        try:
            return os.read(session.fd, size)
        except OSError:
            session.alive = False
            return None

    def write(self, task_id: str, data: bytes) -> bool:
        """Write to PTY master fd."""
        session = self._sessions.get(task_id)
        if not session or not session.alive:
            return False
        try:
            os.write(session.fd, data)
            return True
        except OSError:
            session.alive = False
            return False

    def resize(self, task_id: str, rows: int, cols: int) -> bool:
        """Resize the PTY terminal."""
        session = self._sessions.get(task_id)
        if not session or not session.alive:
            return False
        try:
            import struct
            import fcntl
            import termios
            winsize = struct.pack("HHHH", rows, cols, 0, 0)
            fcntl.ioctl(session.fd, termios.TIOCSWINSZ, winsize)
            return True
        except (OSError, ImportError):
            return False

    def is_alive(self, task_id: str) -> bool:
        """Check if PTY process is still running."""
        session = self._sessions.get(task_id)
        if not session or not session.alive:
            return False
        try:
            pid, status = os.waitpid(session.pid, os.WNOHANG)
            if pid != 0:
                session.alive = False
                return False
            return True
        except ChildProcessError:
            session.alive = False
            return False
```

**Step 4: 运行测试验证通过**

Run: `pytest tests/test_pty_manager.py -v`
Expected: ALL PASS

**Step 5: Commit**

```bash
git add claude_flow/pty_manager.py tests/test_pty_manager.py
git commit -m "feat(pty-manager): add PTY session lifecycle manager for mini tasks"
```

---

## Task 4: 添加 flask-sock 依赖

**Files:**
- Modify: `pyproject.toml:10-16`

**Step 1: 更新 pyproject.toml**

在 `[project.optional-dependencies]` 中添加 `web` 分组，保持 CLI-only 安装轻量：

```toml
[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov"]
web = ["flask>=2.0", "flask-sock>=0.7"]
```

**Step 2: 安装依赖**

Run: `pip install -e ".[web]"`
Expected: flask + flask-sock 安装成功

**Step 3: 验证导入**

Run: `python -c "from flask_sock import Sock; print('OK')"`
Expected: 输出 `OK`

**Step 4: Commit**

```bash
git add pyproject.toml
git commit -m "feat(deps): add flask-sock in [web] optional deps for WebSocket support"
```

---

## Task 5: 下载 xterm.js 静态文件

**Files:**
- Create: `claude_flow/web/static/xterm/xterm.min.js`
- Create: `claude_flow/web/static/xterm/xterm.css`
- Create: `claude_flow/web/static/xterm/addon-fit.min.js`

**Step 1: 下载 xterm.js 核心文件**

```bash
mkdir -p claude_flow/web/static/xterm

# xterm 5.x core
curl -L "https://cdn.jsdelivr.net/npm/@xterm/xterm@5/lib/xterm.min.js" \
  -o claude_flow/web/static/xterm/xterm.min.js

curl -L "https://cdn.jsdelivr.net/npm/@xterm/xterm@5/css/xterm.css" \
  -o claude_flow/web/static/xterm/xterm.css

# fit addon (auto-resize terminal to container)
curl -L "https://cdn.jsdelivr.net/npm/@xterm/addon-fit@0/lib/addon-fit.min.js" \
  -o claude_flow/web/static/xterm/addon-fit.min.js
```

**Step 2: 验证文件存在且非空**

Run: `ls -la claude_flow/web/static/xterm/`
Expected: 3 个文件，每个大于 1KB

**Step 3: 基本校验**

Run: `head -c 100 claude_flow/web/static/xterm/xterm.min.js`
Expected: 包含 JavaScript 代码（不是 HTML 404 页面）

**Step 4: Commit**

```bash
git add claude_flow/web/static/xterm/
git commit -m "feat(static): add xterm.js 5.x local static files for mini task terminal"
```

---

## Task 6: WebSocket 路由 + Mini Task REST API

**Files:**
- Modify: `claude_flow/web/app.py:1-61`
- Create: `claude_flow/web/ws.py`
- Modify: `claude_flow/web/api.py`（在末尾追加 mini task API 端点）
- Test: `tests/test_web_api.py`（追加 mini task API 测试）

### Step 1: 写失败测试

在 `tests/test_web_api.py` 末尾新增：

```python
# -- Mini Task API ----------------------------------------------------------

class TestMiniTaskAPI:
    def test_create_mini_task(self, client):
        resp = client.post(
            "/api/mini-tasks",
            json={"title": "Fix typo", "prompt": "Fix README typo"},
        )
        data = resp.get_json()
        assert resp.status_code == 201
        assert data["ok"] is True
        assert data["data"]["task_type"] == "mini"
        assert data["data"]["status"] == "pending"

    def test_create_mini_task_missing_title(self, client):
        resp = client.post(
            "/api/mini-tasks",
            json={"prompt": "Fix README"},
        )
        assert resp.status_code == 400

    def test_list_mini_tasks(self, client, tm):
        tm.add("Standard task", "standard prompt")
        tm.add_mini("Mini 1", "mini prompt 1")
        tm.add_mini("Mini 2", "mini prompt 2")

        resp = client.get("/api/mini-tasks")
        data = resp.get_json()
        assert data["ok"] is True
        assert len(data["data"]) == 2
        assert all(t["task_type"] == "mini" for t in data["data"])

    def test_mini_task_diff(self, client, tm):
        """GET /api/mini-tasks/<id>/diff returns diff content."""
        t = tm.add_mini("Diff test", "prompt")
        # Manually set branch and status
        def _set():
            tasks = tm._load()
            for task in tasks:
                if task.id == t.id:
                    task.branch = f"cf/{t.id}"
                    task.status = TaskStatus.MERGING
                    tm._save(tasks)
                    return
        tm._with_lock(_set)

        with patch("claude_flow.web.api.subprocess") as mock_sub:
            mock_result = MagicMock()
            mock_result.stdout = "diff --git a/file.py b/file.py\n+new line"
            mock_result.returncode = 0
            mock_sub.run.return_value = mock_result
            resp = client.get(f"/api/mini-tasks/{t.id}/diff")

        data = resp.get_json()
        assert data["ok"] is True
        assert "diff" in data["data"]
```

### Step 2: 运行测试验证失败

Run: `pytest tests/test_web_api.py -v -k "MiniTask"`
Expected: FAIL — `/api/mini-tasks` 路由不存在

### Step 3: 创建 WebSocket 模块

创建 `claude_flow/web/ws.py`：

```python
"""WebSocket handler for mini task PTY terminals.

Uses flask-sock to bridge browser xterm.js with server-side PTY sessions.
Each WebSocket connection maps to one PTY session via task_id.
"""
from __future__ import annotations

import json
import logging
import os
import select
import threading

logger = logging.getLogger(__name__)


def register_ws_routes(sock, app):
    """Register WebSocket routes on the flask-sock instance."""

    @sock.route("/ws/terminal/<task_id>")
    def terminal(ws, task_id: str):
        """Bidirectional WebSocket bridge to a PTY session.

        Protocol:
        - Client -> Server: raw terminal input (UTF-8 text)
        - Server -> Client: raw terminal output (UTF-8 text)
        - Client -> Server: JSON control: {"type": "resize", "rows": N, "cols": N}
        - Server -> Client: JSON status:  {"type": "status", "alive": bool}
        """
        pty_mgr = app.config.get("PTY_MANAGER")
        if not pty_mgr:
            ws.send(json.dumps({"type": "error", "message": "PTY manager not available"}))
            return

        session = pty_mgr.get_session(task_id)
        if not session:
            ws.send(json.dumps({"type": "error", "message": f"No PTY session for {task_id}"}))
            return

        if not session.alive:
            ws.send(json.dumps({"type": "status", "alive": False}))
            return

        # Send initial prompt if set
        if session.prompt:
            prompt_to_send = session.prompt
            session.prompt = ""
            import time
            time.sleep(1)
            pty_mgr.write(task_id, (prompt_to_send + "\n").encode())

        # Background reader: PTY stdout -> WebSocket
        stop_event = threading.Event()

        def _read_pty():
            while not stop_event.is_set():
                try:
                    readable, _, _ = select.select([session.fd], [], [], 0.1)
                    if readable:
                        data = os.read(session.fd, 4096)
                        if data:
                            try:
                                ws.send(data.decode("utf-8", errors="replace"))
                            except Exception:
                                break
                        else:
                            session.alive = False
                            try:
                                ws.send(json.dumps({"type": "status", "alive": False}))
                            except Exception:
                                pass
                            break
                except OSError:
                    session.alive = False
                    break

        reader = threading.Thread(target=_read_pty, daemon=True)
        reader.start()

        # Main loop: WebSocket input -> PTY stdin
        try:
            while True:
                message = ws.receive()
                if message is None:
                    break

                # Try JSON control message
                try:
                    ctrl = json.loads(message)
                    if isinstance(ctrl, dict) and ctrl.get("type") == "resize":
                        pty_mgr.resize(task_id, ctrl.get("rows", 24), ctrl.get("cols", 80))
                        continue
                except (json.JSONDecodeError, ValueError):
                    pass

                # Raw terminal input
                if isinstance(message, str):
                    pty_mgr.write(task_id, message.encode())
                elif isinstance(message, bytes):
                    pty_mgr.write(task_id, message)
        finally:
            stop_event.set()
            reader.join(timeout=2)
```

### Step 4: 在 api.py 末尾添加 Mini Task REST 端点

在 `claude_flow/web/api.py` 文件末尾（`_update_plan_mode` 函数之后）添加：

```python
# -- Mini Task endpoints ----------------------------------------------------

@api_bp.route("/mini-tasks", methods=["GET"])
def list_mini_tasks():
    """List all mini tasks."""
    tm = current_app.config["TASK_MANAGER"]
    tasks = tm.list_tasks(task_type="mini")
    return _ok([t.to_dict() for t in tasks])


@api_bp.route("/mini-tasks", methods=["POST"])
def create_mini_task():
    """Create a mini task. body: {title, prompt}"""
    tm = current_app.config["TASK_MANAGER"]
    data = request.get_json(silent=True)

    if not data:
        return _err("request body cannot be empty")

    title = data.get("title")
    prompt = data.get("prompt", "")

    if not title:
        return _err("title is required")

    task = tm.add_mini(title, prompt)
    return _ok(task.to_dict()), 201


@api_bp.route("/mini-tasks/<task_id>/start", methods=["POST"])
def start_mini_task(task_id: str):
    """Start a mini task: create worktree, start PTY session, mark running."""
    import subprocess
    from datetime import datetime

    tm = current_app.config["TASK_MANAGER"]
    task = tm.get(task_id)

    if not task:
        return _err(f"Task {task_id} not found", 404)
    if task.task_type != "mini":
        return _err(f"Task {task_id} is not a mini task")
    if task.status not in (TaskStatus.PENDING, TaskStatus.INTERRUPTED):
        return _err(f"Task {task_id} is {task.status.value}, cannot start")

    root = current_app.config["PROJECT_ROOT"]
    cfg = current_app.config["CF_CONFIG"]
    pty_mgr = current_app.config.get("PTY_MANAGER")

    if not pty_mgr:
        return _err("PTY manager not available", 500)

    # Create worktree
    branch = f"cf/{task_id}"
    wt = WorktreeManager(root, root / cfg.worktree_dir)
    try:
        wt_path = wt.create(task_id, branch, config=cfg)
    except subprocess.CalledProcessError as e:
        return _err(f"Worktree creation failed: {e.stderr}", 500)

    # Update task status and branch
    tm.update_status(task_id, TaskStatus.RUNNING)
    def _set_branch():
        tasks = tm._load()
        for t in tasks:
            if t.id == task_id:
                t.branch = branch
                t.started_at = datetime.now()
                tm._save(tasks)
                return
    tm._with_lock(_set_branch)

    # Start PTY session
    try:
        pty_mgr.create_session(
            task_id, wt_path,
            prompt=task.prompt,
            skip_permissions=cfg.skip_permissions,
        )
    except Exception as e:
        tm.update_status(task_id, TaskStatus.FAILED, f"PTY creation failed: {e}")
        wt.remove(task_id, branch)
        return _err(f"PTY creation failed: {e}", 500)

    updated = tm.get(task_id)
    return _ok({**updated.to_dict(), "ws_url": f"/ws/terminal/{task_id}"})


@api_bp.route("/mini-tasks/<task_id>/stop", methods=["POST"])
def stop_mini_task(task_id: str):
    """Stop PTY session, auto-commit, transition to merging."""
    import subprocess as sp

    tm = current_app.config["TASK_MANAGER"]
    task = tm.get(task_id)

    if not task:
        return _err(f"Task {task_id} not found", 404)
    if task.status != TaskStatus.RUNNING:
        return _err(f"Task {task_id} is {task.status.value}, cannot stop")

    pty_mgr = current_app.config.get("PTY_MANAGER")
    if pty_mgr:
        pty_mgr.remove_session(task_id)

    # Auto-commit changes in worktree
    root = current_app.config["PROJECT_ROOT"]
    cfg = current_app.config["CF_CONFIG"]
    wt_path = root / cfg.worktree_dir / task_id

    if wt_path.exists():
        sp.run(["git", "add", "-A"], cwd=str(wt_path), capture_output=True)
        sp.run(
            ["git", "commit", "-m", f"feat({task_id}): {task.title}", "--no-verify"],
            cwd=str(wt_path), capture_output=True,
        )

    tm.update_status(task_id, TaskStatus.MERGING)
    updated = tm.get(task_id)
    return _ok(updated.to_dict())


@api_bp.route("/mini-tasks/<task_id>/diff", methods=["GET"])
def mini_task_diff(task_id: str):
    """Get git diff for merge preview."""
    import subprocess as sp

    tm = current_app.config["TASK_MANAGER"]
    task = tm.get(task_id)

    if not task:
        return _err(f"Task {task_id} not found", 404)
    if not task.branch:
        return _err(f"Task {task_id} has no branch")

    root = current_app.config["PROJECT_ROOT"]
    cfg = current_app.config["CF_CONFIG"]

    result = sp.run(
        ["git", "diff", f"{cfg.main_branch}...{task.branch}"],
        cwd=str(root), capture_output=True, text=True,
    )
    stat_result = sp.run(
        ["git", "diff", "--stat", f"{cfg.main_branch}...{task.branch}"],
        cwd=str(root), capture_output=True, text=True,
    )

    return _ok({
        "task_id": task_id,
        "diff": result.stdout,
        "stat": stat_result.stdout,
        "branch": task.branch,
    })


@api_bp.route("/mini-tasks/<task_id>/merge", methods=["POST"])
def merge_mini_task(task_id: str):
    """Merge mini task branch, clean up worktree."""
    tm = current_app.config["TASK_MANAGER"]
    task = tm.get(task_id)

    if not task:
        return _err(f"Task {task_id} not found", 404)
    if task.status != TaskStatus.MERGING:
        return _err(f"Task {task_id} is {task.status.value}, must be merging")

    root = current_app.config["PROJECT_ROOT"]
    cfg = current_app.config["CF_CONFIG"]
    wt = WorktreeManager(root, root / cfg.worktree_dir)

    success = wt.merge(
        task.branch, cfg.main_branch, cfg.merge_strategy,
        config=cfg, task_title=task.title, task_prompt=task.prompt,
    )

    if not success:
        tm.update_status(task_id, TaskStatus.FAILED, "Merge conflict")
        return _err("Merge failed due to conflicts")

    wt.remove(task_id, task.branch)
    if cfg.auto_push:
        wt.push(cfg.main_branch)

    tm.update_status(task_id, TaskStatus.DONE)
    updated = tm.get(task_id)
    return _ok(updated.to_dict())


@api_bp.route("/mini-tasks/<task_id>/discard", methods=["POST"])
def discard_mini_task(task_id: str):
    """Discard mini task — remove worktree without merging."""
    tm = current_app.config["TASK_MANAGER"]
    task = tm.get(task_id)

    if not task:
        return _err(f"Task {task_id} not found", 404)

    pty_mgr = current_app.config.get("PTY_MANAGER")
    if pty_mgr:
        pty_mgr.remove_session(task_id)

    if task.branch:
        root = current_app.config["PROJECT_ROOT"]
        cfg = current_app.config["CF_CONFIG"]
        wt = WorktreeManager(root, root / cfg.worktree_dir)
        wt.remove(task_id, task.branch)

    tm.update_status(task_id, TaskStatus.FAILED, "Discarded by user")
    updated = tm.get(task_id)
    return _ok(updated.to_dict())
```

### Step 5: 修改 app.py — 注册 WebSocket 和 PtyManager

用以下内容覆写 `claude_flow/web/app.py`：

```python
"""Flask application factory with WebSocket support for mini task terminals."""
from __future__ import annotations

from pathlib import Path

try:
    from flask import Flask, render_template
except ImportError:
    raise ImportError(
        "Flask is not installed. Run `pip install flask` to enable Web Manager."
    )

from ..chat import ChatManager
from ..config import Config
from ..planner import Planner
from ..pty_manager import PtyManager
from ..task_manager import TaskManager
from ..usage import UsageManager


def create_app(project_root: Path, config: Config) -> Flask:
    """Create and configure the Flask application."""
    app = Flask(
        __name__,
        template_folder=str(Path(__file__).parent / "templates"),
        static_folder=str(Path(__file__).parent / "static"),
    )

    task_manager = TaskManager(project_root)
    plans_dir = project_root / ".claude-flow" / "plans"
    planner = Planner(project_root, plans_dir, config)
    chat_manager = ChatManager(project_root, config)
    pty_manager = PtyManager()

    app.config["PROJECT_ROOT"] = project_root
    app.config["CF_CONFIG"] = config
    app.config["TASK_MANAGER"] = task_manager
    app.config["PLANNER"] = planner
    app.config["CHAT_MANAGER"] = chat_manager
    app.config["USAGE_MANAGER"] = UsageManager(project_root, config)
    app.config["PTY_MANAGER"] = pty_manager

    # Register REST API blueprint
    from .api import api_bp
    app.register_blueprint(api_bp)

    # Register WebSocket routes (graceful fallback if flask-sock missing)
    try:
        from flask_sock import Sock
        sock = Sock(app)
        from .ws import register_ws_routes
        register_ws_routes(sock, app)
    except ImportError:
        import logging
        logging.getLogger(__name__).warning(
            "flask-sock not installed, WebSocket terminal disabled"
        )

    # Recover interrupted mini task sessions on startup
    _recover_interrupted_sessions(task_manager, pty_manager)

    @app.route("/")
    def index():
        return render_template("index.html")

    return app


def _recover_interrupted_sessions(tm: TaskManager, pty_mgr: PtyManager) -> None:
    """Mark any running mini tasks as INTERRUPTED on server restart."""
    from ..models import TaskStatus
    running_minis = tm.list_tasks(status=TaskStatus.RUNNING, task_type="mini")
    for task in running_minis:
        tm.update_status(task.id, TaskStatus.INTERRUPTED,
                        "Server restarted, PTY session lost")
```

### Step 6: 运行测试

Run: `pytest tests/test_web_api.py -v`
Expected: ALL PASS

### Step 7: Commit

```bash
git add claude_flow/web/ws.py claude_flow/web/api.py claude_flow/web/app.py tests/test_web_api.py
git commit -m "feat(web): add mini task REST API endpoints and WebSocket terminal bridge"
```

---

## Task 7: CLI 命令 — `cf mini add` / `cf mini list`

**Files:**
- Modify: `claude_flow/cli.py`（在 `respond` 命令之后添加 `mini` group）
- Test: `tests/test_cli.py`

### Step 1: 写失败测试

在 `tests/test_cli.py` 中新增（确保有正确的 import）：

```python
class TestMiniCLI:
    def test_mini_add(self, runner, cf_project):
        """cf mini add creates a mini task."""
        os.environ["CF_PROJECT_ROOT"] = str(cf_project)
        result = runner.invoke(main, ["mini", "add", "Fix typo", "-p", "Fix README typo"])
        assert result.exit_code == 0
        assert "mini" in result.output.lower() or "Fix typo" in result.output

        tm = TaskManager(cf_project)
        tasks = tm.list_tasks(task_type="mini")
        assert len(tasks) == 1
        assert tasks[0].title == "Fix typo"

    def test_mini_list_empty(self, runner, cf_project):
        """cf mini list with no mini tasks."""
        os.environ["CF_PROJECT_ROOT"] = str(cf_project)
        result = runner.invoke(main, ["mini", "list"])
        assert result.exit_code == 0
        assert "No mini tasks" in result.output

    def test_mini_list(self, runner, cf_project):
        """cf mini list shows only mini tasks."""
        os.environ["CF_PROJECT_ROOT"] = str(cf_project)
        tm = TaskManager(cf_project)
        tm.add("Standard", "standard prompt")
        tm.add_mini("Mini 1", "mini prompt")

        result = runner.invoke(main, ["mini", "list"])
        assert result.exit_code == 0
        assert "Mini 1" in result.output
        assert "Standard" not in result.output
```

### Step 2: 运行测试验证失败

Run: `pytest tests/test_cli.py -v -k "Mini"`
Expected: FAIL — `mini` 命令组不存在

### Step 3: 实现 CLI 命令

在 `claude_flow/cli.py` 中，`respond` 命令之后添加：

```python
# -- Mini task commands -----------------------------------------------------

@main.group()
def mini():
    """Manage mini tasks (lightweight interactive tasks)."""
    pass


@mini.command("add")
@click.argument("title")
@click.option("-p", "--prompt", default=None, help="Initial prompt for Claude")
@click.pass_context
def mini_add(ctx, title, prompt):
    """Create a new mini task."""
    root = ctx.obj["root"]
    tm = TaskManager(root)

    if prompt is None:
        prompt = click.edit("# Enter the prompt for Claude (mini task)\n")
        if not prompt:
            click.echo("Aborted: no prompt provided")
            return

    task = tm.add_mini(title, prompt)
    click.echo(f"Mini task created: {task.id} - {task.title}")
    click.echo(f"Open the web manager to start the interactive session.")


@mini.command("list")
@click.pass_context
def mini_list(ctx):
    """List all mini tasks."""
    root = ctx.obj["root"]
    tm = TaskManager(root)
    tasks = tm.list_tasks(task_type="mini")

    if not tasks:
        click.echo("No mini tasks")
        return

    status_icon = {
        "pending": "○", "running": "▶", "merging": "⇄",
        "done": "●", "failed": "✗", "interrupted": "⊘",
    }
    for t in tasks:
        icon = status_icon.get(t.status.value, "?")
        click.echo(f"  {icon} {t.id}  {t.status.value:<12}  {t.title}")
```

### Step 4: 运行测试验证通过

Run: `pytest tests/test_cli.py -v`
Expected: ALL PASS

### Step 5: Commit

```bash
git add claude_flow/cli.py tests/test_cli.py
git commit -m "feat(cli): add 'cf mini add' and 'cf mini list' commands"
```

---

## Task 8: 前端 UI — 侧边栏 + 创建对话框 + xterm 终端 + diff 预览

**Files:**
- Modify: `claude_flow/web/templates/index.html`

**这是最大的单一任务。** 修改全在 `index.html` 中。具体修改点：

### 8.1 侧边栏 — 在 `</nav>` 之前（line 1925）、Task Status 区域之后插入

```html
<div class="sidebar-section-label">Mini Tasks</div>
<button class="nav-item" onclick="showMiniCreateDialog()">
    <span class="nav-dot" style="background: #a29bfe;"></span>
    <span class="nav-label">+ New Mini Task</span>
</button>
<div id="miniTaskList"></div>
```

### 8.2 CSS — 在 `<style>` 区域添加 mini task 样式

需要添加的样式类：
- `.mini-task-nav-item` — 侧边栏 mini task 按钮
- `.mini-status-dot.{pending|running|merging|done|failed|interrupted}` — 状态点颜色
- `.mini-create-dialog` / `.mini-create-form` — 创建弹窗
- `.mini-terminal-container` / `.mini-terminal-header` / `.mini-terminal-body` — 终端容器
- `.mini-diff-container` / `.diff-add` / `.diff-del` / `.diff-header` — diff 预览

### 8.3 HTML 组件 — 在 Usage Dashboard 之前添加

1. **创建对话框** (`miniCreateDialog`) — 弹窗表单，标题 + prompt 输入
2. **终端视图** (`miniTerminalView`) — xterm.js 终端容器 + 顶部操作栏（Stop/Discard）
3. **Diff 预览视图** (`miniDiffView`) — diff 内容 + Merge/Discard 按钮

### 8.4 JavaScript — 在 `<script>` 区域末尾添加

核心函数：
- `loadXtermScript(callback)` — 动态加载 xterm.js 和 fit addon
- `refreshMiniTaskList()` — 拉取 `/api/mini-tasks` 渲染侧边栏列表
- `showMiniCreateDialog()` / `closeMiniCreateDialog()` — 弹窗控制
- `createMiniTask()` — 调用 `POST /api/mini-tasks` 创建任务
- `selectMiniTask(taskId)` — 点击侧边栏 mini task 时的入口：
  - pending/interrupted → 调用 `/start`，打开终端
  - running → 直接打开终端（reattach）
  - merging → 显示 diff 预览
- `openTerminal(taskId, title)` — 初始化 xterm.js + WebSocket 连接
- `showDiffPreview(taskId, title)` — 拉取 diff 并渲染
- `stopMiniTask()` — 调用 `/stop`，切换到 diff 预览
- `mergeMiniTask()` — 调用 `/merge`
- `discardMiniTask()` — 调用 `/discard`
- `escapeHtml(text)` — HTML 转义工具函数
- 定时刷新：`setInterval(refreshMiniTaskList, 5000)`

**完整的 JavaScript 代码见 Task 8 中的代码块（约 200 行），需要整体插入到 index.html 的 `<script>` 闭合标签之前。**

### Step 5: 手动验证

Run: `cf web` → 打开 `http://localhost:8080`

验证清单：
1. 侧边栏 Task Status 之后出现 "Mini Tasks" section 和 "+ New Mini Task" 按钮
2. 点击 "+ New Mini Task" 弹出创建对话框
3. 填写标题和 prompt 后点击 Create，任务出现在侧边栏
4. 点击 mini task 条目，终端视图打开，xterm.js 正确渲染
5. 点击 "Stop & Review" 后终端关闭，显示 diff 预览
6. 点击 "Merge" 完成合并，任务状态变为 done

### Step 6: Commit

```bash
git add claude_flow/web/templates/index.html
git commit -m "feat(frontend): add mini task UI — sidebar, create dialog, terminal, diff preview"
```

---

## Task 9: 集成测试 — Mini Task 完整生命周期

**Files:**
- Create: `tests/test_mini_task_integration.py`

### Step 1: 创建集成测试文件

```python
"""Mini task integration tests — full lifecycle verification.

PTY operations are mocked since they require a real terminal.
Tests: create → start → stop → diff → merge → done
"""
from __future__ import annotations

from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

from claude_flow.config import Config
from claude_flow.models import Task, TaskStatus
from claude_flow.task_manager import TaskManager


@pytest.fixture
def mini_app(cf_project):
    """Flask test app with mocked PtyManager."""
    from claude_flow.web.app import create_app

    cfg = Config.load(cf_project)
    app = create_app(cf_project, cfg)
    app.config["TESTING"] = True

    # Replace PtyManager with a mock
    mock_pty = MagicMock()
    mock_pty.get_session.return_value = MagicMock(alive=True, prompt="")
    mock_pty.create_session.return_value = MagicMock(
        task_id="test", pid=1234, fd=5, alive=True
    )
    app.config["PTY_MANAGER"] = mock_pty
    return app


@pytest.fixture
def mini_client(mini_app):
    return mini_app.test_client()


@pytest.fixture
def mini_tm(mini_app) -> TaskManager:
    return mini_app.config["TASK_MANAGER"]


class TestMiniTaskLifecycle:
    def test_create_via_api(self, mini_client):
        resp = mini_client.post(
            "/api/mini-tasks",
            json={"title": "Test mini", "prompt": "Fix something"},
        )
        data = resp.get_json()
        assert data["ok"]
        assert data["data"]["task_type"] == "mini"
        assert data["data"]["status"] == "pending"

    def test_isolation_from_standard_tasks(self, mini_client, mini_tm):
        mini_tm.add("Standard", "prompt")
        mini_tm.add_mini("Mini", "prompt")

        resp = mini_client.get("/api/mini-tasks")
        data = resp.get_json()
        assert len(data["data"]) == 1
        assert data["data"][0]["title"] == "Mini"

    def test_start_creates_worktree_and_pty(self, mini_client, mini_tm, mini_app):
        t = mini_tm.add_mini("Start test", "test prompt")

        with patch("claude_flow.web.api.WorktreeManager") as mock_wt_cls:
            mock_wt = MagicMock()
            wt_dir = Path(mini_app.config["PROJECT_ROOT"]) / ".claude-flow/worktrees" / t.id
            wt_dir.mkdir(parents=True, exist_ok=True)
            mock_wt.create.return_value = wt_dir
            mock_wt_cls.return_value = mock_wt

            resp = mini_client.post(f"/api/mini-tasks/{t.id}/start")
            assert resp.get_json()["ok"]

        task = mini_tm.get(t.id)
        assert task.status == TaskStatus.RUNNING

    def test_interrupted_on_restart(self, mini_tm, cf_project):
        t = mini_tm.add_mini("Running", "prompt")
        mini_tm.update_status(t.id, TaskStatus.RUNNING)

        from claude_flow.web.app import _recover_interrupted_sessions
        from claude_flow.pty_manager import PtyManager

        _recover_interrupted_sessions(mini_tm, PtyManager())

        updated = mini_tm.get(t.id)
        assert updated.status == TaskStatus.INTERRUPTED

    def test_backward_compat_old_tasks(self, mini_tm):
        t = mini_tm.add("Old task", "old prompt")
        assert t.task_type == "standard"

        minis = mini_tm.list_tasks(task_type="mini")
        assert not any(task.id == t.id for task in minis)
```

### Step 2: 运行测试

Run: `pytest tests/test_mini_task_integration.py -v`
Expected: ALL PASS

### Step 3: 运行全量测试

Run: `pytest -v`
Expected: ALL PASS

### Step 4: Commit

```bash
git add tests/test_mini_task_integration.py
git commit -m "test(mini-task): add integration tests for full mini task lifecycle"
```

---

## Task 10: 文档更新

**Files:**
- Modify: `CLAUDE.md`
- Modify: `claude_flow/CLAUDE.md`

### Step 1: 更新根级 CLAUDE.md

在"任务生命周期"部分添加 Mini Task 分支:

```
Mini Task: pending --> running (interactive PTY) --> merging --> done
                                                             \-> failed
                   interrupted <-- (server restart)
```

在"CLI 入口"部分添加:

```
cf mini add "标题" -p "prompt"   # 创建 mini task
cf mini list                    # 列出 mini tasks
```

### Step 2: 更新 claude_flow/CLAUDE.md

在"核心类"表中添加:
| `PtyManager` | `pty_manager.py` | PTY session 生命周期管理 |
| `PtySession` | `pty_manager.py` | 单个 PTY 会话数据模型 |

在"CLI 命令结构"中添加:
```
 |-- mini (group)
 |    |-- add                  # 创建 mini task
 |    |-- list                 # 列出 mini tasks
```

在"外部依赖"表中添加:
| `flask-sock` | WebSocket 支持（mini task 终端） |

在"相关文件清单"中添加:
| `pty_manager.py` | ~ | PTY 进程管理器 |
| `web/ws.py` | ~ | WebSocket 终端桥接 |

### Step 3: Commit

```bash
git add CLAUDE.md claude_flow/CLAUDE.md
git commit -m "docs: update CLAUDE.md with mini task feature documentation"
```

---

## 任务依赖图

```
Task 1 (models) ─┬──> Task 2 (TaskManager) ──> Task 7 (CLI)
                  │
                  └──> Task 3 (PtyManager)  ──┐
                                              │
Task 4 (flask-sock) ──> Task 6 (API+WS) ─────┤
                                              │
Task 5 (xterm.js) ──> Task 8 (Frontend) ──────┤
                                              │
                                              └──> Task 9 (Integration) ──> Task 10 (Docs)
```

**可并行的任务组：**
- Group A: Task 1 → Task 2（串行）
- Group B: Task 3（独立）
- Group C: Task 4 + Task 5（独立，可并行）
- 以上三组可同时开始

**汇合点：** Task 6 依赖 Task 2 + 3 + 4；Task 8 依赖 Task 5 + 6

---

## 预估时间

| Task | 预估 | 累计 |
|------|------|------|
| Task 1: 数据模型 | 10 min | 10 min |
| Task 2: TaskManager | 10 min | 20 min |
| Task 3: PtyManager | 20 min | 40 min |
| Task 4: flask-sock | 5 min | 45 min |
| Task 5: xterm.js | 5 min | 50 min |
| Task 6: API + WebSocket | 25 min | 75 min |
| Task 7: CLI | 10 min | 85 min |
| Task 8: Frontend UI | 30 min | 115 min |
| Task 9: 集成测试 | 10 min | 125 min |
| Task 10: 文档 | 10 min | 135 min |

**总预估：约 2 小时 15 分钟**
